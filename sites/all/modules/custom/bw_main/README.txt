Main BeWelcome dependencies can go into this otherwise empty module.

The BeWelcome install profile depends on this module so the profile can remain relatively empty.

TODO: add an install hook that is always triggered during updatedb and
makes sure dependencies are enabled.
